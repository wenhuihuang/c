#include <stdio.h>

void main(){
    float x;
    int n;
    char str[100];
    /* scanf("%s",str);
    printf("输入的字符是:%s",str); */
    //scanf("%f%d",&x,&n); //scanf 以空为结束
    //printf("输入的数字是%f,%d",x,n);

    char s[10] = "abcdddddd";
    printf("%d\n",sizeof(s));
}